{% extends 'base.html.twig' %}

{% block title %}{{ contest.title }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-12">
            <h1>{{ contest.title }}</h1>
        </div>
    </div>
{% endblock %}
